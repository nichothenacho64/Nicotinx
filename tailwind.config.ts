export default {
    theme: {
        extend: {
            colors: {
                blue: {
                    default: "var(--blue-default)",
                },
            },
        },
    },
};
